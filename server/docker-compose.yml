version: "3.8"

services:
  gyst-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - HOST=0.0.0.0
      - PORT=8080
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_MODEL=claude-3-5-haiku-20241022
      - RUST_LOG=info
    restart: unless-stopped
